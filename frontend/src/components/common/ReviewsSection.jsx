import { Quote, Star } from "lucide-react";
import { motion } from "framer-motion";

export default function ReviewsSection({ reviews = [] }) {
  return <section id="reviews" className="landing-band scroll-mt-20 border-y border-white/10 bg-[#081C14] py-16 lg:py-20"><div className="page-wrap"><p className="text-[11px] font-bold uppercase tracking-[0.2em] text-leaf">Community reviews</p><h2 className="mt-3 text-3xl font-black sm:text-5xl">Trusted by the people moving produce forward.</h2>{reviews.length ? <div className="mt-9 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{reviews.map((review, index) => <motion.article key={review._id} initial={{ opacity: 0, y: 14 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.06 }} className="landing-card border border-white/10 bg-panel p-6"><Quote className="h-6 w-6 text-leaf/70" /><div className="mt-5 flex items-center gap-3"><div className="grid h-11 w-11 shrink-0 place-items-center overflow-hidden rounded-full border border-white/15 bg-[#123225] font-bold text-leaf">{review.buyer?.profileImage ? <img src={review.buyer.profileImage} alt={review.buyer.name} className="h-full w-full object-cover" /> : (review.buyer?.name || "K").slice(0, 1).toUpperCase()}</div><div className="min-w-0"><p className="truncate font-bold">{review.buyer?.name || "KRISHIQ customer"}</p><p className="text-xs text-[#94A3B8]">{review.product?.name || "Verified marketplace order"}</p></div></div><div className="mt-4 flex gap-1" aria-label={`${review.rating} out of 5 stars`}>{[1, 2, 3, 4, 5].map((star) => <Star key={star} className={`h-4 w-4 ${star <= review.rating ? "fill-[#FBBF24] text-[#FBBF24]" : "text-slate-600"}`} />)}</div><p className="mt-4 text-sm leading-relaxed text-[#C7D5CE]">“{review.comment}”</p></motion.article>)}</div> : <p className="mt-7 max-w-xl text-[#A7B8B0]">Verified buyer reviews will appear here after completed orders are rated.</p>}</div></section>;
}